import {SvgThemeColorDirective} from "./svg-theme-color.directive";


describe('SvgThemeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgThemeColorDirective();
    expect(directive).toBeTruthy();
  });
});
